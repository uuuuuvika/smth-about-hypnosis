# Env file for rpi-video-viewer systemd service
# Copy this to /etc/default/rpi-video-viewer and edit values as needed

# LED matrix options (without sudo). Example:
# --led-chain=2 --led-cols=64 --led-rows=32 --led-gpio-mapping=adafruit-hat
VIDEO_VIEWER_OPTS="--led-chain=2 --led-cols=64 --led-rows=32 --led-gpio-mapping=adafruit-hat"

# Decoder thread count (1..N)
VIDEO_THREADS=2

# One or more inputs: files or directories (space-separated)
# Example: a directory with mp4s
VIDEO_INPUTS="/home/pi/py/optimized-videos"
